

import 'package:flutter/material.dart';

class contineroneline extends StatelessWidget {
  const contineroneline({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      //for logo line and list
      width: 15,
      height: 1,
      color: Colors.blue[50],
    );
  }
}